version: '3.8'

services:
  server:
    build:
      context: ..
      dockerfile: tests/echo_server/server/Dockerfile
    volumes:
      - ../../webserv:/app
    networks:
      - webserv-net

  client:
    build:
      context: ..
      dockerfile: tests/echo_server/client/Dockerfile
    volumes:
      - ../../webserv:/app
    networks:
      - webserv-net

networks:
  webserv-net:
